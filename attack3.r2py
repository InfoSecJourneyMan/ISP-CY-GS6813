# Read directly to wrong offset 

if "testfile.txt.a" in listfiles():
  removefile("testfile.txt.a")
if "testfile.txt.b" in listfiles():
  removefile("testfile.txt.b")

file=ABopenfile("testfile.txt",True)

try:
    file.readat(2,-4)
except RepyArgumentError:
    pass
else:
    log("Error Attack")
finally:
    exitall()

myfile.close()
